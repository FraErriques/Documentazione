2023 July 04
----------------

RustBriko is a Solution (i.e. a SandBox or WorkSpace) devoted to implement the multi-layer model in Rust language.
It makes use of the Cargo package-tool and the standard library crates.
The setup on Windows is guaranteed by MingW gcc, which provides the linker (link.exe).
