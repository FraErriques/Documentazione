
{
  "version": "0.2.0",
  "configurations": [

    {
      "name": "Cargo launch",
      "type": "lldb",
      "request": "launch",

      // ✅ Use the project’s debug binary automatically (optional)
      // If you prefer your absolute path, keep it. This just avoids edits per project.
      "program": "${workspaceFolder}/target/debug/${workspaceFolderBasename}.exe",

      "args": [],
      "cwd": "${workspaceFolder}",

      "MIMode": "lldb",
      //"miDebuggerPath": "C:/Users/itfraerr/program/msys64/mingw64/bin/gdb.exe",

      // 🔽 Key settings to see and step in disassembly
       "showDisassembly": "auto",       // "auto" to switch only when source missing
       "disassemblyFlavor": "intel",      // Optional: if you prefer Intel syntax
       "stopAtEntry": false,

      // Optional but handy: keep MSYS2 tools on PATH when starting under GUI
      // (Safe to remove if you always launch VS Code from MINGW64 terminal.)
      "environment": [
        { "name": "PATH", "value": "C:/Users/itfraerr/program/msys64/mingw64/bin;${env:PATH}" }
      ],

      // If you want a separate console for stdin/stdout:
      // "externalConsole": true,
      
      "setupCommands": [
        { "text": "-enable-pretty-printing", "description": "Enable GDB pretty printing", "ignoreFailures": true },

        // Show the next line’s assembly while stepping
        { "text": "set disassemble-next-line on", "description": "Show asm for next line", "ignoreFailures": true }
      ]
    }
  ]
}
